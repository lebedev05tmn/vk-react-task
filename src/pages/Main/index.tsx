import { FC } from "react";
import { Layout } from "widgets";
import { MainList } from "widgets/MainList";

const MainPage: FC = () => {
  return (
    <Layout>
      <MainList />
    </Layout>
  );
};

export { MainPage };
